{
  "private": true,
  "devDependencies": {
    "autoprefixer": "^6.4.1",
    "babel-cli": "^6.11.4",
    "babel-preset-es2015": "^6.13.2",
    "babelify": "^7.3.0",
    "browser-sync": "^2.14.0",
    "browserify": "^13.1.0",
    "clean-css": "^3.4.19",
    "cssnano": "^3.7.5",
    "faucet": "0.0.1",
    "node-sass": "^3.8.0",
    "npm-run-all": "^2.3.0",
    "onchange": "^2.5.0",
    "tape": "^4.6.0",
    "uglifyify": "^3.0.2"
  },
  "scripts": {
    "server": "browser-sync start -s 'dist' -f 'dist'",
    "cleanup": "rm -rf dist/*",
    "setupfolder": "mkdir -p dist/scripts dist/styles dist/images",
    "test": "babel-node src/tests/*.js | (faucet || true) && echo ------------- ",
    "build:html": "cp -r src/index.html dist/",
    "build:scripts": "browserify -x jquery -x bootstrap -x toastr -x masonry-layout src/scripts/*.js -o dist/scripts/main.js",
    "build:images": "cp -rf src/images/* dist/images/",
    "build:vendor": "browserify -r jquery -r bootstrap -r toastr -r masonry-layout -o dist/scripts/vendor.js && cat node_modules/bootstrap/dist/css/bootstrap.min.css node_modules/toastr/build/toastr.css | cleancss -o dist/styles/bundle.css && cp node_modules/bootstrap/dist/css/bootstrap.min.css.map dist/styles/bootstrap.min.css.map",
    "build:scss": "node-sass --sourceMap=dist/styles/main.css.map --outFile=dist/styles/main.css src/styles/main.scss -o dist/styles",
    "build:css": "postcss --local-plugins -u autoprefixer -u cssnano dist/styles/main.css -o dist/styles/main.min.css",
    "build:styles": "npm run build:scss && npm run build:css",
    "watch:html": "onchange src/**/*.html -- npm run build:html",
    "watch:js": "onchange src/**/*.js -e src/test/**/*.js -- npm run build:scripts",
    "watch:styles": "onchange src/styles/*.scss -- npm run build:styles",
    "test:watch": "onchange src/**/*.js -- npm run test",
    "build": "npm run cleanup && npm run setupfolder && npm run build:vendor && npm run build:html && npm run build:scripts && npm run build:styles && npm run build:images",
    "start": "npm run build && run-p watch:* -s server",
    "start:testing": "npm test && npm run test:watch -s"
  },
  "dependencies": {
    "bootstrap": "^3.3.7",
    "jquery": "^3.1.0",
    "jquery-bridget": "^2.0.1",
    "masonry-layout": "^4.1.1",
    "toastr": "^2.1.2"
  },
  "name": "portfolio_page",
  "version": "1.0.0",
  "main": "index.html",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/PortableStick/portfolio_page.git"
  },
  "author": "Gregory Smith",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/PortableStick/portfolio_page/issues"
  },
  "homepage": "https://github.com/PortableStick/portfolio_page#readme",
  "description": "",
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": [
            "es2015"
          ]
        }
      ],
      [
        "uglifyify"
      ]
    ]
  }
}
